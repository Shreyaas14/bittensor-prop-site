services:
  - type: web
    name: bittensor-app
    env: node
    buildCommand: npm install && cd frontend && npm install
    startCommand: npm run render-start
    envVars:
      - key: NODE_ENV
        value: production 