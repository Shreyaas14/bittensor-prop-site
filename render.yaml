services:
  - type: web
    name: bittensor-app
    env: node
    buildCommand: npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production 